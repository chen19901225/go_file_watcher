{
        "filewatcher.commands": [
            {
                "match": "\\.go",
                "isAsync": false,
                "cmd": "cd ${workspaceRoot} && go build && go install",
                "event": "onFileChange"
            }        
        ]
}